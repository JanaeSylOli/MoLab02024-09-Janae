import SwiftUI

struct UtilitiesView: View {
    var body: some View {
        NavigationWrapper {
            VStack {
                Text("Utilities Page")
                    .font(.largeTitle)
                    .padding()
            }
        }
    }
}

